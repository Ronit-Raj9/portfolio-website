# GitHub API Token (for higher rate limits and accessing private data)
# Create a token at https://github.com/settings/tokens with 'repo' and 'read:user' scopes
GITHUB_TOKEN=your_personal_access_token_here 